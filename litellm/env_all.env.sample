LITELLM_PROVIDER= # optional default provider (openai|gemini|deepseek)

# OpenAI
OPENAI_API_KEY=

# Gemini (Google Vertex AI) - path to service account JSON if using ADC
GOOGLE_API_KEY=
# Or mount service account JSON and set this path inside the container
GOOGLE_APPLICATION_CREDENTIALS=/secrets/google-service-account.json

# DeepSeek
DEEPSEEK_API_KEY=

# Development fallback: if set, adapter will return a canned response instead of calling providers
DEBUG_LITELLM_FAKE_RESP=0
